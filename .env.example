DATABASE_URL="postgresql://user:password@localhost:5432/smart_shopping_list?schema=public"
JWT_SECRET="your_jwt_secret_key_here"
PORT=3001
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001

# Instruções importantes:
# 1. Copie este arquivo para .env (backend) e .env.local (frontend)
# 2. Substitua os valores pelas suas configurações reais
# 3. NUNCA commite arquivos .env com dados reais para o repositório
# 4. Para produção, use credenciais seguras e conexão SSL no banco
